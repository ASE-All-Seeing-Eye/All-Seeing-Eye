[project]
name = "backend"
version = "0.1.0"
description = "Backend for All Seeing Eye"
authors = [
    { name = "Salih Hane", email = "2202@htl.rennweg.at" }
]
requires-python = ">=3.13"
dependencies = [
    "sqlalchemy>=2.0.52",
]

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
